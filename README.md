# Airport Problem

## Описание

В файле(во вложении) задан список аэродромов, для аэродрома заданы имя (4 буквы) и гео-координаты (в системе координат WGS-84).

Нужно написать GUI-приложение со следующим функционалом:
1) отображает данные из файла в виде таблицы
2) находит все аэродромы ближе N км к выбранному в таблице (N задается в пользовательском интерфейсе) и выводит список аэродромов.
3) находит кратчайший путь между двумя произвольно заданными аэродромами при условии, что самолёт может из любого аэродрома перелететь в любой, но не может пролететь без посадки более M км (M так же задается в пользовательском интерфейсе). Результат представить в виде списка промежуточных аэродромов.

## Технологии

-   **C++20**
-   **Qt 6 (Core, Widgets)**
-   **CMake 3.16+**
-   **GoogleTest** 
-   **lcov / gcov** 


## Зависимости

### Ubuntu
```bash
sudo apt update sudo apt install -y\
build-essential\
cmake\
qt6-base-dev\
qt6-tools-dev\
libgtest-dev\
lcov
```

### Coverage
```bash
sudo apt install lcov gcovr
```

## Сборка проекта

### Только приложение
```bash
./scripts/build.sh
./build/src/app
```
### Приложение + тесты
```bash
./scripts/build_and_test.sh
```
### Покрытие
```bash
./scripts/coverage.sh
```

## Docker для тестов
```
docker build -t airport .
docker run --rm airport
```
------------------------------------------------------------------------

